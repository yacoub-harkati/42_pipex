#include <stdio.h>
#include <string.h>
#include "../includes/libft.h"

int main(int ac, char **av, char **env)
{

	// 	char *args[4];

	// 	args[0] = "ls";
	// 	args[1] = "-a";
	// 	args[2] = "-l";
	// 	args[3] = NULL;
	// 	if (execve("/usr/bin/ls", args, env) < 0)
	// 	{
	// 		perror("Error");
	// 		exit(1);
	// 	}
	printf("%zu\n", sizeof(char *****));
	printf("%zu\n", sizeof(char *****));
}